<%= form_for(@widget) do |f| %>
  <% if @widget.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@widget.errors.count, "error") %> prohibited this widget from being saved:</h2>

      <ul>
      <% @widget.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :widgetable_id %><br />
    <%= f.text_field :widgetable_id %>
  </div>
  <div class="field">
    <%= f.label :widgetable_type %><br />
    <%= f.text_field :widgetable_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
